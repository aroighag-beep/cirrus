# cirrus

Cirrus SR22-G6 Turbo listings scraper.

Manually trigger the **Scrape Cirrus Listings** workflow from the Actions tab.
The scraper fetches all pages from Controller EMEA, updates `cirrus_data.json`, and commits the result.
